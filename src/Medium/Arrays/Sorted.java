package Medium.Arrays;

import java.util.Arrays;
public class Sorted {
    //make a method called sort() and let it take in a int[] and return the sorted version of that array
    //code your own sorting algorithm (search some up, pick one, try understanding how it works, then code it in the method)
    public static void main(String[] args) {
        int[] nums1 = {5,4,3,2,1};
        int[] nums2 = {2342,52346,123,16,574,12,134,4};
//
//        System.out.println(Arrays.equals(sort(nums1), Arrays.sort(nums1)));
//        System.out.println(Arrays.equals(sort(nums2), Arrays.sort(nums2)));
    }
    //make the method below


}
